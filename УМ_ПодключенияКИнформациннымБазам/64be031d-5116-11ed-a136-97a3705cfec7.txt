{
 'Реквизиты': {
  'Владелец': 'e1cib/data/Справочник.УМ_ИнформационныеБазы?ref=a13697a3705cfec711ed511620327b59',
  'Наименование': 'API: HTTPСоединение и заголовки',
  'Код1СДляПодключения': 'СтрокаАвторизации = "[user]:[password]";   //в качестве пароля используется апи токен

//base64
   	Поток = Новый ПотокВПамяти;
    Запись = Новый ЗаписьДанных(Поток, КодировкаТекста.UTF8);
    Запись.ЗаписатьСимволы(СтрокаАвторизации);
    Запись.Закрыть();
    ДвоичныеДанные = Поток.ЗакрытьИПолучитьДвоичныеДанные();
    СтрокаФорматBase64 = Base64Строка(ДвоичныеДанные);
    СтрокаФорматBase64 = СтрЗаменить(СтрокаФорматBase64, Символы.ВК, "");// убираем ВК
    СтрокаФорматBase64 = СтрЗаменить(СтрокаФорматBase64, Символы.ПС, "");// убираем ПС 
    
Заголовки = Новый Соответствие;
Заголовки.Вставить("Authorization","Basic "+СтрокаФорматBase64);
Заголовки.Вставить("Content-Type", "application/json");   
   
   
ИспользоватьАутентификациюОС = Ложь;
HTTPСоединение = Новый HTTPСоединение(
		"[server]", // сервер (хост)
		443, // порт, по умолчанию для http используется 80, для https 443
		, // пользователь для доступа к серверу (если он есть)
		, // пароль для доступа к серверу (если он есть)
		, // здесь указывается прокси, если он есть
		, // таймаут в секундах, 0 или пусто - не устанавливать
		Новый ЗащищенноеСоединениеOpenSSL,  // защищенное соединение, если используется https
		ИспользоватьАутентификациюОС); 
		
		
Соединение =  Новый Структура("HTTPСоединение,Заголовки",HTTPСоединение,Заголовки);',
  'ТипПодключения': 'HTTP'
 },
 'ТЧ': [
  {
   'Переменные': []
  }
 ]
}
